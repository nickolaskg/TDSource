# Browser-safe values only. Never expose service-role or OAuth secrets with VITE_ prefixes.
VITE_ENABLE_DEMO_MODE=false

# Server-side values and secrets belong in .dev.vars for local development and
# in Cloudflare Worker variables/secrets for deployed environments. See
# .dev.vars.example for the complete list.
